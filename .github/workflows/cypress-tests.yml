name: E2E Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      test_suite:
        description: 'Select test suite to run'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - pl
          - en
      environment:
        description: 'Select environment'
        required: true
        default: 'prod'
        type: choice
        options:
          - prod
          - staging
          - dev

jobs:
  cypress-tests:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Determine test spec
        id: spec
        run: |
          if [ "${{ github.event.inputs.test_suite }}" = "pl" ]; then
            echo "spec=cypress/e2e/dcg-homepage.cy.js" >> $GITHUB_OUTPUT
          elif [ "${{ github.event.inputs.test_suite }}" = "en" ]; then
            echo "spec=cypress/e2e/dcg-homepage-en.cy.js" >> $GITHUB_OUTPUT
          else
            echo "spec=cypress/e2e/*.cy.js" >> $GITHUB_OUTPUT
          fi

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        with:
          browser: firefox
          spec: ${{ steps.spec.outputs.spec }}
          cache-key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        env:
          CYPRESS_ENVIRONMENT: ${{ github.event.inputs.environment || 'prod' }}
          CYPRESS_BASE_URL: ${{ vars.CYPRESS_BASE_URL || 'https://dcg.pl' }}

      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-screenshots
          path: cypress/screenshots
          retention-days: 7

      - name: Upload test report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cypress-html-report
          path: cypress/reports/html
          retention-days: 30

      - name: Upload videos
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cypress-videos
          path: cypress/videos
          retention-days: 3
          if-no-files-found: ignore
